name: Cowsay Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  cowsay:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run cowsay script
        run: bash scripts/cowsay.sh
